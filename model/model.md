% Level1.m requires a model and a contrast function to specify the GLM. %

% See Model_[model_name].m for model specification of the GLM.
% See Contrast_[model_name].m for regressors of interests of the GLM
